import { HttpRequestOptions, Method, HttpResponse } from './types';
export declare class Request {
    constructor();
    request(url: string, method: Method, options?: HttpRequestOptions): Promise<HttpResponse>;
}
